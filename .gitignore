# Dependencies
node_modules/
__pycache__/
*.pyc
.venv/
venv/

# Build output
frontend/dist/

# IDE
.vscode/
.idea/
.cursor/*
!.cursor/skills/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.local
cloud-migration-it-handover.md

# litellm-callbacks tests
k8s/litellm-callbacks/tests/.venv/
k8s/litellm-callbacks/__pycache__/
k8s/litellm-callbacks/tests/__pycache__/

# Large binaries / installers (don't commit to repo)
*.dmg
docs/QNX_UT_Training_v2.pptx
docs/*.tar.gz

# Raw data exports (large + may contain sensitive info)
docs/litellm-raw-*.csv
docs/her-distillations/

# Local invoice manifests/previews may contain filenames, hashes, and amounts.
invoice-rollup/
invoice-rollup-*/
docs/feishu-invoice-rollup-*.json
docs/feishu-invoice-rollup-*.xml

# Claude Code: project-level settings.json is committed,
# but local-only overrides stay out of the repo
.claude/settings.local.json
.224-pre-reinstall-*/
scripts/chatgpt-quota-engine-web/web/.env.local

# Local operator secrets (sudo password, master keys). Never commit.
# Create with scripts/lib/carher-secrets-init.sh
.carher-secrets.json
.gstack/
docs/assets/.preview/

# zk-delta 采集到的真实 Cursor body —— 里面是你的真实会话正文和代码，绝不入库
# 整个目录一起挡：原先只写 *.json，而 _manifest.jsonl 不匹配 *.json，差一个字母就漏出去了
zk-delta/tests/fixtures/

# 改密/凭据落盘记录。2026-09-20 发现 scripts/chatgpt-onboard/zerokey-codex/
# zerokey-patch/.tmp-larkdl/newpw.dat 是 64 行邮箱 + 成文的新密码生成规则，mode 0600，
# 未被任何规则挡住 —— 一次 `git add .` 就会把它推上去，而凭据一旦进历史，
# 摘当前树 ≠ 消除暴露，只能轮转。它从未被提交过，这几条是为了让它进不去。
*newpw*
.tmp-larkdl/
**/.tmp-larkdl/
*.dat
.pytest_cache/

# S1 执行卡:cursor-g 网页线基线(2026-08-26 起)

> 角色分工:**你(liuguoxian04 in Cursor)发轮次,我收口径**。101/key03 与同事流量零打扰。

## 你要做的(分散在正常使用里攒就行,不用一口气)

用 **Cursor + 04 账户**,模型选 `cursor-g-5.6-sol`(或 sol-high,记一下用的哪个),攒 ≥30 轮,三类都要有:

| 类型 | 目标轮数 | 例子 |
|---|---|---|
| 纯聊天(不需要工具) | ~10 | "解释一下 X"、"这段报错什么意思" |
| 单工具(一次命令能完) | ~10 | "ls 一下当前目录"、"看下 git status" |
| 多工具(要连跑几步) | ~10 | "找到 X 文件并统计行数"、"跑测试并总结失败原因" |

**第 1 轮特殊**:先发一句 `运行 echo S1PIN-0826 并告诉我输出`——我用它在 pod 日志里核对你的流量钉在哪条 lane(WA 亲和实测,S2 尾巴)。

每轮**不用记笔记**,只有当你体感"这轮不对劲"(近空/装聋/答非所问/特别慢)时,顺手记个大概时间点发我即可。

## 我要做的(你攒完或攒到一半喊我)

1. 拉 `zero-cursor-bpi-82` + `zero-cursor-bpi` pod 日志(`--tail=-1`,覆盖你的使用窗口),
   逐轮按**三分法**归类:{完整终止 | 显式失败 | 无终止半截},半截再打形态子标签(近空/半信封/哑火);
2. 与你的体感时间点交叉验证;凑出「r16 后真实失败率 + 形态分布」基线表;
3. **过分叉门**:失败率已很低 → 方案降级/搁置回你拍板;仍显著 → 进 S3。

## 口径纪律(防假数据)

- 每一轮必须在 pod 日志里找到对应记录才算数(打错 lane 的轮次单独标出,不进基线);
- SpendLogs 本线 token/model_id 全空(合成 usage 老坑),只用它对时间戳;
- pod 重启会丢日志窗口——你若发现 pod 重启过,提前喊我落盘一次。

## 已备好的资产

- codex 线回归:`scripts/zk-cursor-web/codex_regress.py`,golden 基线
  `198:/home/cltx/backups-bpi/codex_regress_baseline_20260826_0044.json`
  (两轮均带 tools;**已知既有怪癖**:无 tools 请求该线只发 added 就断流无 completed——真实流量不走此形状,已排除在回归外);
- S2 侦察结论:`docs/cursor-g-s2-findings-20260826.md`(五处施工图/金丝雀=82/推荐克隆第三 deploy 方案)。
